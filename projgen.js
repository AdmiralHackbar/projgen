var noun = ['pet', 'news', 'hotel', 'travel']
var how = ['aggregator', 'provider', 'service', 'auction']


function page(span) {

    var nounIndex = Math.floor(Math.random() * noun.length);
    var howIndex = Math.floor(Math.random() * how.length);

};